[uwsgi]
chdir = /srv/server/
module = polls.wsgi:application
home = /home/ubuntu/venv/

uid = deploy
gid = deploy

http = :8080

enable-threads = true
master = true
vacuum = true
pidfile = /tmp/polls.pid
logto = /var/log/uwsgi/polls/@(exec://date +%%Y-%%m-%%d).log
log-reopen = true